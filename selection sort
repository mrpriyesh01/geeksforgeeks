def selecction(arr1):
     n=len(arr1)
     for i in range(n,len(arr1)-1):
            mini-i
            for j n range(i+1,n):
                 if arr1[j]< arr1[mini]:
                         mini=j
                         arr1[j],arr1[mini]=arr1[mini],arr1[j]

