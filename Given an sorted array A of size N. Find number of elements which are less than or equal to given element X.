QUESTION= Given an sorted array A of size N.
Find number of elements which are less than or equal to given element X.




code=

def countOfElements(a, n, x):
    count = 0
    for i in range(0, n):
        if a[i] <= x:  
            count += 1
    return count
