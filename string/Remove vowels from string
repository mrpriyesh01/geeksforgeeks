Remove vowels from string
Difficulty: SchoolAccuracy: 41.92%Submissions: 38K+Points: 0
Given a string, remove the vowels from the string.

Input: S = "welcome to geeksforgeeks"
Output: wlcm t gksfrgks
Explanation: Ignore vowels and print other
characters 
code=
class Solution:
	def removeVowels(self, S):
	    vowels = "aeiouAEIOU"  # Define the vowels
        result = ""  # Initialize an empty string for the result

        for char in S:  # Loop through each character in the input string
            if char not in vowels:  # Check if the character is not a vowel
                result += char  # Add the character to the result if it's not a vowel

        return result  
	   
